请结合文案的ASR识别结果，设计一套用于短视频的重点强调词，这些重点强调词用于在短视频画面上显示以引导观众观看。

要求：
1. 关键词提取思路：
- 诗名
- 时间地点（例如：元和十年的暮春）
- 人物描述 （例如：北宋婉约派的代表人物）
- 关键动词 

2. 每个内容需要标注重要等级和类型：
   - type: 内容类型
     - 0: 诗名（如"江雪"）
     - 1: 诗句（如"千山鸟飞绝"、"孤舟蓑笠翁", 并且连续的上下句不能拆开，上下句用换行符连接（如"千山鸟飞绝\\n万径人踪灭"））
     - 2: 其它关键词句（控制在 2-8 个字为宜，最多不超过8个字）
   - level: 关键词重要等级
     - 1 - 10: 最高为1，最低为10
     - 其他关键词：1-10级，根据重要性结合全文重点词的适宜密度，给一个等级
   - begin_time: 关键词开始时间(精确到关键词开始的begin_time)
   - end_time: 关键词结束时间(精确到关键词结束的end_time)
3. 关于end_time的设置规则：
 - 关键词持续时间为：字数乘以250毫秒，例如，”ABCD“四个字，持续时间应该为4*250=1000毫秒
 - type增益：type=0或type=1时，持续时长须再乘以2，且不能少于4000毫秒
 - 确保以上规则的同时，end_time还要合理设置，确保不超过下一个keyword的begin_time，必要时要对关键词进行取舍，保证当前keyword的显示时间有良好观感
4. 返回格式为 JSON 数组，每个元素包含 keyword、level、type、begin_time、end_time 字段，且元素必须按begintime递增排序
5. 只返回 JSON 数组，不要其他内容

开始思考分析并设计强调词（返回JSON数组）：